---
interface Props {
  as?: 'div' | 'section' | 'main' | 'article' | 'header' | 'footer';
  class?: string;
}

const { as: Tag = 'section', class: className = '' } = Astro.props;
---

<Tag class={`max-w-5xl w-full mx-auto px-4 md:px-8 ${className}`}>
  <slot />
</Tag>
